/**
 * @file register.tsx
 * @description Register route page (Expo Router)
 */

import { RegisterScreen } from '@/features/auth';

export default function RegisterPage() {
  return <RegisterScreen />;
}
